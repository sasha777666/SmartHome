
&НаСервере
Процедура ВыполнитьИтерациюПолучитьТекущееДанныеНаСервере()
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	ОбработкаОбъект.ПроцессДляСервераПолучитьТекущиеДанные(); 
	ЗначениеВРеквизитФормы(ОбработкаОбъект, "Объект");
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьИтерациюПолучитьТекущееДанные(Команда)
	ВыполнитьИтерациюПолучитьТекущееДанныеНаСервере();
КонецПроцедуры
