

&НаСервере
Процедура ПроцессДляСервераОднократноНаСервере()
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	ОбработкаОбъект.ПроцессДляСервера(); 
	ЗначениеВРеквизитФормы(ОбработкаОбъект, "Объект");
КонецПроцедуры

&НаКлиенте
Процедура ПроцессДляСервераОднократно(Команда)
	ПроцессДляСервераОднократноНаСервере();
КонецПроцедуры
