
&НаСервере
Процедура ПроцессДляСервераНаСервере()
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	ОбработкаОбъект.ПроцессДляСервера(); 
	ЗначениеВРеквизитФормы(ОбработкаОбъект, "Объект");
КонецПроцедуры

&НаКлиенте
Процедура ПроцессДляСервера(Команда)
	ПроцессДляСервераНаСервере();
КонецПроцедуры
