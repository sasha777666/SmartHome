
Функция ПриёмЭлементаОбработчик(Запрос)
	
	СтрокаСДанными = Запрос.ПолучитьТелоКакСтроку();
	Структура = ЗначениеИзСтрокиВнутр(СтрокаСДанными);
	
	Параметры = Новый Массив();
	Параметры.Добавить(Структура);
	
	ОбщегоНазначения.ВыполнитьПроцедуруФункциюВФонеНаСервере("Интеграции.ПринятьДанныеИзСтруктуры", Параметры);
	
	Ответ = Новый HTTPСервисОтвет(200);
	Возврат Ответ;
	
КонецФункции

