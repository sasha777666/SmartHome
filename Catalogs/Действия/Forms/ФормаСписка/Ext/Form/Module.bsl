
&НаСервере
Процедура ВыполнитьДействияВФонеНаСервере()
	
	мПараметр = Новый Массив();
	мПараметр.Добавить(Элементы.Список.ВыделенныеСтроки);
	
	ОбщегоНазначения.ВыполнитьПроцедуруФункциюВФонеНаСервере("Справочники.Действия.ВыполнитьДействия", мПараметр);	 
					
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьДействияВФоне(Команда)
	ВыполнитьДействияВФонеНаСервере();
КонецПроцедуры
